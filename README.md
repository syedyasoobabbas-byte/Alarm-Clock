# Alarm-Clock
A Python command-line alarm clock that works in Indian Standard Time (IST) and accepts alarm times in 12-hour format (e.g. 07:30 AM). Displays a live clock and rings an alert when the target time is reached.
